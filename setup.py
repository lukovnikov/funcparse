from setuptools import setup, find_packages

setup(name="funcparse",
      description="funcparse",
      author="Sum-Ting Wong",
      author_email="sumting@wo.ng",
      install_requires=[],
      packages=["funcparse"],
      )
